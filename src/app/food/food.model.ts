export class Food {
  public id: string;
  public name: string;
  public content: string;

  constructor(id: string, name: string, content: string) {
    this.id = id;
    this.name = name;
    this.content = content;
  }
}
